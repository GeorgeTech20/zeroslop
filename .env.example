# Copiar a .env.local y completar con valores reales. .env* está en .gitignore.
# NUNCA commitear valores reales ni pegarlos en un chat.

# Clerk (dashboard.clerk.com → API keys)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx

# Convex (lo escribe solo `npx convex dev`)
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
